# 第 14 轮前端审查归档（2026-06-11）

> 来源：ISSUES.md 第 14 轮登记，全部已修复并验证通过。

## 修复清单

### MAJOR（2 条）

1. **`frontend/src/pages/Portfolio/index.tsx:164,265` + `TrackedAssets/index.tsx:155` — confirmDelete + remove.mutate() 死代码**
   - `mutate()` fire-and-forget 使 ConfirmDelete 的 try/catch 成死代码
   - 改为 `mutateAsync()` 让错误能被 ConfirmDelete 正确捕获并提示

2. **`frontend/src/pages/AssetDetail/index.tsx:30-33` — refresh() 在 invalidateQueries 完成前显示成功消息**
   - `invalidateQueries` 返回 Promise 未被 await
   - 改为 async + await + try/catch，成功才提示"已刷新"

### MINOR（3 条）

3. **`frontend/src/pages/AssetDetail/index.tsx:283` — ShareholderTab rowKey 碰撞风险**
   - `rowKey={(r) => r.name}` → `rowKey={(_, i) => "shareholder-${i}"}`

4. **`frontend/src/pages/AssetDetail/index.tsx:223` — IntradayTab rowKey 碰撞风险**
   - `rowKey="time"` → `rowKey={(_, i) => "minute-${i}"}`

5. **`frontend/src/components/layout/HealthIndicator.tsx:12` — 不安全 as 断言**
   - `error.response?.data as Partial<HealthResponse>` → `isDegradedPayload()` 类型守卫函数进行运行时验证

### NIT（1 条）

6. **`frontend/src/pages/AssetDetail/index.tsx:51` — 刷新按钮无防抖**
   - 加 `loading={detail.isFetching}` 防止双击

## 验证结果

- `uv run ruff check .` — All checks passed
- `uv run pytest tests/ -v` — 495 passed, 1 warning
- `npm run type-check` — 无错误