# 第 13 轮 React 迁移审查归档（2026-06-11）

> 来源：ISSUES.md 第 13 轮登记，全部已修复并验证通过。

## 修复清单

### CRITICAL（2 条）

1. **`frontend/src/pages/AssetDetail/index.tsx` — 刷新 mutation 不 invalidate query，UI 显示旧数据**
   - IntradayTab / ShareholderTab 的 `useMutation` 在 `onSuccess` 中添加 `queryClient.invalidateQueries()`

2. **`frontend/src/api/client.ts` — 401 拦截器无条件清空 API key 且不引导用户**
   - 默认 API key 不再持久化到 localStorage（`apiKeyStore.ts` 改为空字符串默认值）
   - 401 拦截器改用 `showErrorMessage()` context bridge 而非 `message.error()` 静态调用

### MAJOR（13 条）

3. **AssetDetail useQuery 使用 POST 做读操作** → 改用 GET 端点 + mutation 用 POST `/refresh`
4. **Sidebar selectedKeys 无法匹配子路由** → 最长前缀匹配 `matchesRoute()`
5. **ConfirmDelete onConfirm 异步错误被吞** → `try/catch + showErrorMessage + re-throw`
6. **formatPercent 与 formatNumber 不一致** → 统一用 `toLocaleString("zh-CN")` + `normalizeFiniteNumber()` 守卫
7. **HealthIndicator "degraded" 显示为连接失败** → 三态：ok/绿、degraded/黄、error/红
8. **Settings + TaskStatus 非成功状态统一标红** → `getTaskStatusMeta()` 集中映射
9. **types.ts 联合类型末尾 `| string` 使类型检查失效** → 删除 `| string`，改用严格枚举
10. **raw_data 迁移无事务保护** → `try/finally` FK 恢复 + `BEGIN/COMMIT/ROLLBACK` + `PRAGMA table_info`
11. **main.py Provider 关闭访问私有成员** → 新增 `close_providers()` 公开方法
12. **launcher Vite 输出完全丢弃** → 日志重定向到 `vite-dev.log`
13. **launcher watch_task 通过名称查找不可靠** → 直接引用局部变量
14. **AssetDetail quote `prev_close`/`amount` 字段缺失** → 后端 CTE 补选 + 前端类型对齐 + 测试断言

### MINOR（7 条）

15. **App.tsx 无 ErrorBoundary** → `RouteErrorBoundary` 组件 + `createPageRoute()` 工厂
16. **MotionCard/MotionPage 未尊重 prefers-reduced-motion** → `useReducedMotion()` 条件渲染
17. **Portfolio 行 rowKey 可能不唯一** → `position-${account_id}-${symbol}` / `realized-${account_id}-${symbol}`
18. **client.ts 使用 antd 静态 `message.error`** → `AppMessageBridge` + `bindAppMessageApi()` context bridge
19. **apiKeyStore 默认 key 持久化** → 默认值空字符串，`getApiKey()` 回退 `DEFAULT_API_KEY`
20. **AppLayout 硬编码后端 URL** → `import.meta.env.VITE_API_BASE_URL || "/api/v1"`
21. **formatNumber 不处理 Infinity** → `normalizeFiniteNumber()` 守卫
22. **useHealthCheck 无 refetchIntervalInBackground: false** → 增加 `usePageVisible()` + 轮询暂停

### NIT（4 条）

23. **launcher `_resolve_project_root` 两分支返回相同值** → frozen 分支改为 `Path.cwd()`
24. **launcher `"browser_task" in locals()` 判断** → 顶部声明 + `is not None` 检查
25. **PnlDisplay 缺少 aria-label** → `aria-label` 辅助标签
26. **StatusTag null 与真实数据不可区分** → `<span aria-label="未设置">未设置</span>`
27. **AppLayout Emoji 未标记装饰性** → `<span aria-hidden="true">📊 </span>`

## 验证结果

- `uv run ruff check .` — All checks passed
- `uv run pytest tests/ -v` — 495 passed, 1 warning
- `npm run type-check` — 无错误