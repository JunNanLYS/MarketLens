# MarketLens 开发计划

## Summary

第一版目标是做一个本地优先的 **AI 金融研究助理**：用户维护追踪标的，系统自动采集结构化金融数据和新闻，入库沉淀，再由 AI 基于证据生成分析报告。第一版不做自动交易、不接实盘下单。

默认技术栈：

- 后端：Python + FastAPI
- 数据库：SQLite
- 调度：APScheduler
- 数据处理：pandas
- 页面：Streamlit
- 数据源：WeStockData 为主，新浪财经/公开源为补充，NeoData 作为可选增强
- AI：结构化 JSON 输出，必须引用已采集证据

## 1. 项目底座

- 初始化项目结构：`backend/`、`backend/collectors/`、`backend/services/`、`backend/storage/`、`backend/scheduler/`、`ui/`、`docs/`、`data/`、`tests/`。
- 建立配置系统：数据库路径、WeStock 包名、调用超时、新闻 RSS、NeoData 开关。
- 建立 SQLite 初始化流程。
- 建立运行日志表，记录任务成功、失败、耗时和错误细节。

核心表：

- `tracked_assets`：追踪标的
- `market_quotes`：实时行情
- `kline_daily`：日 K 数据
- `financial_reports`：财报数据
- `fund_flows`：资金流数据
- `technical_indicators`：技术指标
- `news_items`：新闻/公告/研报
- `ai_reports`：AI 分析报告
- `run_logs`：任务运行记录

## 2. 追踪标的层

- 实现添加、删除、启用、停用、查看追踪标的。
- 每个标的保存：代码、名称、市场、资产类型、数据源、标签、备注、创建时间、更新时间。
- 支持资产类型：股票、ETF、指数、期货、外汇、商品。
- 第一版优先支持股票、ETF、指数；期货/外汇/商品先保留字段和接口。

API：

- `POST /assets`
- `GET /assets`
- `GET /assets/{id}`
- `PATCH /assets/{id}`
- `DELETE /assets/{id}`
- `POST /assets/search`

## 3. 数据层

- 封装 `WeStockProvider`，通过 Python `subprocess` 调用 `westock-data-clawhub@1.0.4`。
- 第一阶段采集：搜索、行情、K线、财报、资金流、技术指标。
- 保存原始返回和标准化数据，避免后续无法追溯。
- 建立统一数据源接口：`search()`、`quote()`、`kline()`、`finance()`、`fund_flow()`、`technical()`。
- 新浪财经/公开源作为行情或新闻补充，不作为唯一主数据源。

采集顺序：

1. 读取启用的追踪标的
2. 按资产类型选择 provider
3. 采集原始数据
4. 标准化字段
5. 写入数据库
6. 写入运行日志

## 4. 新闻层

- 实现新闻采集器，按追踪标的采集相关新闻。
- 新闻数据保存标题、来源、发布时间、链接、正文/摘要、关联标的、情绪、重要性。
- 第一版先做关键词关联：名称、股票代码、行业关键词。
- 后续再加入向量检索和更复杂的实体识别。
- NeoData 若可用，则作为新闻、公告、研报、宏观事件增强源；不可用时不阻塞主流程。

## 5. 调度层

- 使用 APScheduler 建立后台任务。
- 支持手动触发采集和定时采集。
- 第一版默认任务：

| 任务 | 频率 |
|---|---|
| 行情采集 | 每 15 分钟 |
| K线/资金/技术指标 | 每日收盘后 |
| 新闻采集 | 每小时 |
| AI 分析报告 | 每日晚上 |

- 每次任务写入 `run_logs`，记录成功、失败、耗时、错误信息。

## 6. AI 层

- AI 不直接凭空判断，只读取数据库中的行情、资金、技术、财务、新闻证据。
- 建立 `EvidenceBuilder`，为每个标的生成 AI 输入证据包。
- 建立 `AIAnalyzer`，输出固定 JSON。
- 第一版内置保守规则型分析器，后续可替换为真实 LLM 调用。

AI 输出格式：

```json
{
  "symbol": "hk00700",
  "action": "watch",
  "confidence": 0.68,
  "risk_level": "medium",
  "summary": "短期趋势震荡，新闻偏正面，资金面尚未确认。",
  "bullish_reasons": [],
  "bearish_reasons": [],
  "key_risks": [],
  "data_used": [],
  "generated_at": "2026-05-31T20:00:00+08:00"
}
```

允许动作：

- `buy`
- `sell`
- `watch`
- `avoid`

## 7. 用户层

- 使用 Streamlit 实现第一版界面。
- 页面包括：

| 页面 | 功能 |
|---|---|
| 追踪列表 | 添加、删除、启停、标签管理 |
| 标的详情 | 行情、K线摘要、财务摘要、资金流 |
| 新闻中心 | 按标的查看相关新闻 |
| AI 报告 | 查看每日分析、理由、风险、置信度 |
| 任务状态 | 查看采集任务运行结果 |
| AI 问答 | 基于本地数据提问 |

- 用户问答必须基于数据库数据和 AI 报告回答，不直接编造实时信息。

## Test Plan

- 追踪标的：添加、重复添加、删除、停用、搜索失败。
- 数据采集：单标的采集、多个标的采集、数据源失败、超时、空数据。
- 新闻采集：重复新闻去重、无新闻、多个标的关联同一新闻。
- 调度任务：手动触发、定时触发、失败重试、日志写入。
- AI 分析：证据不足、新闻偏正面、资金流偏负面、输出 JSON 格式校验。
- 用户层：添加标的后能看到数据、新闻和 AI 报告；任务失败时页面有明确提示。

验收标准：

- 可以添加至少 5 个追踪标的。
- 可以自动采集行情、K线、新闻。
- 可以保存并查看历史数据。
- 可以生成每个标的的 AI 分析报告。
- AI 报告必须显示使用了哪些数据。
- 数据源失败不导致整个系统崩溃。

## 当前实现状态

- 已完成项目结构、SQLite schema、追踪标的服务、WeStock 采集封装、新闻 RSS 采集入口、调度任务、证据聚合、规则型 AI 分析、FastAPI 接口和 Streamlit 页面。
- 下一步建议：安装依赖后启动 API 和 Streamlit，添加 3 到 5 个标的进行采集验证。
