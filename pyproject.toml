[project]
name = "marketlens"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "apscheduler",
    "pandas",
    "loguru",
    "pyyaml",
    "httpx",
    "streamlit",
    "pydantic",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "httpx",
]
